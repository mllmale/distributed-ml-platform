services:
  minio:
    image: minio/minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: euzinha
      MINIO_ROOT_PASSWORD: senhazona123
    command: server /data --console-address ":9001"